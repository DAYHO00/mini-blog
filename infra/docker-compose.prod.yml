services:
  db:
    image: postgres:16
    container_name: mini-blog-db
    networks:
      - mini-blog-net
    environment:
      POSTGRES_DB: miniblog
      POSTGRES_USER: miniblog
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: always

  backend:
    image: ghcr.io/dayho00/mini-blog-api:latest
    container_name: mini-blog-backend
    networks:
      - mini-blog-net
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_HOST: mini-blog-db
      DB_PORT: 5432
      DB_NAME: miniblog
      DB_USER: miniblog
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      - db
    restart: always

  frontend:
    image: mini-blog-web:local
    container_name: mini-blog-frontend
    networks:
      - mini-blog-net
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always

networks:
  mini-blog-net:
    driver: bridge

volumes:
  db_data:
